<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "mybatis-3-mapper.dtd" "mybatis-3-mapper.dtd" >
<mapper namespace="online.qsx.mapper.UsersMapper">
	<select id="getUsers" resultType="online.qsx.model.Users"  parameterType="online.qsx.model.Users" >
		SELECT * FROM student WHERE `id` =  #{id}
	</select>

	<select id="getUseries" resultType="online.qsx.model.Users"  parameterType="online.qsx.model.Users" >
		SELECT * FROM student
	</select>
</mapper>
